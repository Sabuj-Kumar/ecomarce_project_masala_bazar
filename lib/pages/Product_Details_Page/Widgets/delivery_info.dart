import 'package:flutter/material.dart';

class DeliveryInfo extends StatelessWidget {
  const DeliveryInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: const [
        Text("okay boss murder done."),
        Text("10kg fish murder done bro hehe."),
        Text("Masala Bazer kidnap. ebar free te khabo ja ase. sodo ca kheye ki r hoy.")
      ],
    );
  }
}
